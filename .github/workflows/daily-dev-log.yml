name: Daily Dev Log

on:
  schedule:
    # Three legitimate check-ins per day: plan, progress, summary.
    - cron: "15 14 * * *"
    - cron: "15 19 * * *"
    - cron: "15 23 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-log:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Update daily dev log check-in
        run: python scripts/update_dev_log.py

      - name: Commit changes if needed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add logs README.md scripts .github/workflows
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "docs: update dev log check-in $(date -u +%F-%H%M)"
          git push
